{
    "configurations": [
        {
            "type": "aws-sam",
            "request": "direct-invoke",
            "name": "markChoreComplete:markChoreComplete",
            "invokeTarget": {
                "target": "template",
                "templatePath": "${workspaceFolder}/lambda/markChoreComplete/template.yaml",
                "logicalId": "markChoreComplete"
            },
            "lambda": {
                "payload": { "json": { "queryStringParameters": { "preview":"true", "asOfDate": "1664582520163" } } },
                "environmentVariables": {}
            }
        }
    ]
}